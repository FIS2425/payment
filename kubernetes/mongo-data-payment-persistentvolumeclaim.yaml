apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: mongo-data-payment
  name: mongo-data-payment
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
