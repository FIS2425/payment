apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: mongo-payment
  name: mongo-payment
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
